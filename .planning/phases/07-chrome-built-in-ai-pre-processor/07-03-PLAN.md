---
phase: 07-chrome-built-in-ai-pre-processor
plan: 03
wave: 3
depends_on:
  - 07-02
files_modified:
  - apps/chrome-extension/src/background/service-worker.ts
  - apps/chrome-extension/src/options/options.tsx
  - apps/chrome-extension/src/offscreen/__tests__/chrome-ai-processor.test.ts
autonomous: true
requirements:
  - CHROME-01
  - CHROME-02
  - CHROME-03
---

# Plan 07-03: UI de Status de Fallback, Background Integration & Testes Unitários

## Objectives
Conectar o status do Gemini Nano no Background Service Worker e na página de Opções da Extensão, além de criar testes unitários para a classe `ChromeBuiltInAIProcessor` e validar o build de todo o monorepo.

## Tasks

<task id="07-03-01" type="code">
  <name>Atualizar Service Worker e Página de Opções com o Status do Gemini Nano</name>
  <read_first>
    - apps/chrome-extension/src/background/service-worker.ts
    - apps/chrome-extension/src/options/options.tsx
    - .planning/phases/07-chrome-built-in-ai-pre-processor/07-CONTEXT.md
  </read_first>
  <action>
    1. Em apps/chrome-extension/src/background/service-worker.ts:
       - Tratar mensagens de atualização de status do pré-processador Chrome AI e armazenar no chrome.storage.local ou estado da sessão.
    2. Em apps/chrome-extension/src/options/options.tsx (ou componente de configurações):
       - Exibir uma seção de status de IA On-device:
         - "Gemini Nano: Ativo (On-device)" quando window.ai estiver operacional.
         - "Gemini Nano: Indisponível (Bypass Ativo)" quando window.ai não estiver presente ou estiver indisponível.
  </action>
  <acceptance_criteria>
    - apps/chrome-extension/src/options/options.tsx contém a renderização do status do Gemini Nano (On-device vs Bypass)
    - O Service Worker repassa corretamente as atualizações de estado do pré-processador
  </acceptance_criteria>
</task>

<task id="07-03-02" type="code">
  <name>Criar suíte de testes unitários para o ChromeBuiltInAIProcessor</name>
  <read_first>
    - apps/chrome-extension/src/offscreen/chrome-ai-processor.ts
    - apps/orchestrator/src/services/__tests__/provider-manager.test.ts
  </read_first>
  <action>
    Criar apps/chrome-extension/src/offscreen/__tests__/chrome-ai-processor.test.ts:
    1. Testar checkAvailability quando window.ai é undefined (deve retornar { available: false, status: 'unavailable' }).
    2. Testar correctUtterance em modo bypass (deve retornar o texto original inalterado com corrected: false).
    3. Mockar a API window.ai.languageModel e testar a correção ortográfica de jargões técnicos.
    4. Testar a lógica de gatilho híbrido de sumarização (disparo a cada 5 frases ou 60 segundos).
  </action>
  <acceptance_criteria>
    - O arquivo de teste apps/chrome-extension/src/offscreen/__tests__/chrome-ai-processor.test.ts é criado
    - Todos os testes unitários executam e passam via npm test
  </acceptance_criteria>
</task>

<task id="07-03-03" type="command">
  <name>Executar validação completa do monorepo (Build & Testes)</name>
  <read_first>
    - package.json
    - AGENTS.md
  </read_first>
  <action>
    Executar a suíte completa de verificação do monorepo:
    1. npm run build:types
    2. npm test
    3. npm run build:extension
    4. npm run build:orchestrator
  </action>
  <acceptance_criteria>
    - Todos os comandos de build e testes finalizam com exit code 0 sem erros TypeScript ou regressões
  </acceptance_criteria>
</task>
