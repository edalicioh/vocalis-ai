---
phase: 07-chrome-built-in-ai-pre-processor
plan: 01
wave: 1
depends_on: []
files_modified:
  - packages/shared-types/src/messages.ts
  - packages/shared-types/src/index.ts
autonomous: true
requirements:
  - CHROME-01
  - CHROME-02
---

# Plan 07-01: Tipos & Protocolo do Pré-processador Local

## Objectives
Estender a biblioteca de tipos compartilhados `@conversation-copilot/shared-types` com as estruturas de dados e eventos WebSocket necessários para o suporte ao pré-processador local Chrome Built-in AI.

## Tasks

<task id="07-01-01" type="code">
  <name>Adicionar interfaces de dados do pré-processador local</name>
  <read_first>
    - packages/shared-types/src/messages.ts
    - packages/shared-types/src/index.ts
  </read_first>
  <action>
    Em packages/shared-types/src/messages.ts:
    1. Adicionar o tipo ProcessedTranscriptPayload contendo:
       - originalText: string
       - correctedText: string
       - incrementalSummary?: string
       - category?: string
       - isBypass: boolean
    2. Adicionar o tipo de mensagem WebSocket 'transcript.preprocessed' ao tipo genérico de eventos WS.
    3. Exportar as novas interfaces em packages/shared-types/src/index.ts.
  </action>
  <acceptance_criteria>
    - packages/shared-types/src/messages.ts contém a definição de 'ProcessedTranscriptPayload'
    - packages/shared-types/src/messages.ts contém o evento 'transcript.preprocessed'
    - npm run build:types compila sem erros
  </acceptance_criteria>
</task>

<task id="07-01-02" type="command">
  <name>Compilar pacote shared-types</name>
  <read_first>
    - packages/shared-types/package.json
  </read_first>
  <action>
    Executar npm run build:types para gerar os artefatos de tipo em dist/ do pacote shared-types.
  </action>
  <acceptance_criteria>
    - O comando npm run build:types é concluído com exit code 0
  </acceptance_criteria>
</task>
